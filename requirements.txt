# Core
torch>=2.3.0
torchaudio>=2.3.0
numpy<2.0
scipy
tqdm
pyyaml
click

# YouTube download
yt-dlp>=2024.8.0

# Audio processing
librosa>=0.10.0
soundfile
pydub
webrtcvad

# Transcription / alignment / validation
openai-whisper>=20231117
whisperx>=3.1.0
faster-whisper>=1.0.0

# Hindi text processing
indic-nlp-library>=0.92
num2words>=0.5.13
regex
unicodedata2

# Model training
coqui-tts>=0.22.0
# coqui-tts 0.27.x imports isin_mps_friendly from transformers.pytorch_utils,
# which was removed in transformers 5.x. Pin to the 4.57.x line that
# satisfies coqui's own >=4.57 requirement and still has the symbol.
transformers>=4.57,<5

# Neural metrics
torchmetrics

# Inference API + training studio UI
fastapi
uvicorn
pydantic
python-multipart

# Dev / testing
pytest
pytest-cov
